import React from 'react'
import ReactDOM from 'react-dom/client'
import { MovieNightApp } from './MovieNightApp.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MovieNightApp />
  </React.StrictMode>,
)
